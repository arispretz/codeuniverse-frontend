# Environment type (test, development, production)
VITE_ENV=test

# Firebase configuration (dummy values for testing)
VITE_FIREBASE_API_KEY=firebase_test_key
VITE_FIREBASE_AUTH_DOMAIN=test-app.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=test-project
VITE_FIREBASE_APP_ID=1:1234567890:web:abcdef123456

# Express backend base URL (mocked for tests)
VITE_EXPRESS_URL=http://localhost:4000

# Backend WebSocket host (dummy value)
VITE_BACKEND_WS_HOST=ws://localhost:4000

# Socket server URL (mocked for tests)
VITE_SOCKET_SERVER_URL=http://localhost:5001

# Socket client URL (dummy value)
VITE_SOCKET_URL=ws://localhost:5001

# RapidAPI key (dummy value for testing)
VITE_RAPIDAPI_KEY=rapidapi_test_key

# FastAPI service URL (mocked for tests)
VITE_FASTAPI_URL=http://localhost:6000

# Assistant user ID (dummy value)
VITE_ASSISTANT_USER_ID=test-assistant-id

# Allowed roles for dashboard access (comma-separated list)
VITE_DASHBOARD_ALLOWED_ROLES=ADMIN,MANAGER,USER

# Allowed CORS origins for sockets (dummy localhost)
SOCKET_CORS_ORIGINS=http://localhost:3000
